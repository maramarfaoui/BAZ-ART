{% extends getRole(app.request) == "ROLE_ARTISTE" ? "base-front-artiste.html.twig" :  getRole(app.request) == "ROLE_PRESTATAIRE"  ? 'base-back-prest.html.twig' : 'base-back.html.twig' %}


{% block title %}Edit Evenement{% endblock %}

{% block body %}
    <h1 class="text-primary" style="margin-left: 35%">Modifier Evenement</h1>

    {{ include('event/_form.html.twig', {'button_label': 'Mettre à jour'}) }}<br>

    <a href="{{ path('app_event_index') }}">
        <button class="btn btn-info">Reour en Arrière</button>
    </a><br>

    <br>{{ include('event/_delete_form.html.twig') }}
{% endblock %}
