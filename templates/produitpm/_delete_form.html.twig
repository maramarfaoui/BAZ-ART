<form method="post" action="{{ path('supp_prod', {'id': produitpm.idprod}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input class=" btn btn-primary" type="hidden" name="_token" value="{{ csrf_token('delete' ~ produitpm.idprod) }}">
    <button class=" btn btn-primary" style="color: #ff000b">Delete</button>
</form>
