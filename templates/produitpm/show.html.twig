{% extends getRole(app.request) == "ROLE_ARTISTE" ? "base-back-artiste.html.twig" :  getRole(app.request) == "ROLE_PRESTATAIRE"  ? 'base-back-prest.html.twig' : 'base-back.html.twig' %}


{% block title %}Produitpm{% endblock %}

{% block body %}
    <h1>Produitpm</h1>

    <table class="table" methode="POST">
        <tbody>
        <tr>
            <th>Id Du Produit</th>
            <td>{{ produitpm.idprod }}</td>
        </tr>
        <tr>
            <th>Nom Du produit</th>
            <td>{{ produitpm.nomprod }}</td>
        </tr>
        <tr>
            <th>Reference</th>
            <td>{{ produitpm.referencep }}</td>
        </tr>
        <tr>
            <th>Quantite</th>
            <td>{{ produitpm.quantitep }}</td>
        </tr>
        <tr>
            <th>Type</th>
            <td>{{ produitpm.typep }}</td>
        </tr>
        <tr>
            <th>Prix</th>
            <td>{{ produitpm.prixpm }}</td>

        </tr>
        <tr>
            <th>QRcode</th>
            <td><img class="img-fluid" src="/assets/qr-code/{{ produitpm.qrcode }}"> </td>
        </tr>
        <tr>
            <th>Date d'Ajout</th>
            <td>{{ produitpm.dateajoutpm }}</td>
        </tr>
        </tbody>
    </table>

    <a class=" btn btn-primary" href="{{ path('app_produitpm_index') }}">back to list</a>

    <a  class=" btn btn-primary" href="{{ path('update', {'id': produitpm.idprod}) }}">edit</a>

    {{ include('produitpm/_delete_form.html.twig') }}
{% endblock %}
