{% extends getRole(app.request) == "ROLE_ARTISTE" ? "base-front-artiste.html.twig" :  getRole(app.request) == "ROLE_PRESTATAIRE"  ? 'base-back-prest.html.twig' : 'base-back.html.twig' %}


{% block title %}Edit Eventcalendar{% endblock %}

{% block body %}
    <h1 class="text-primary" style="margin-left: 35%">Edit Eventcalendar</h1>

    {{ include('event_calendar/_form.html.twig', {'button_label': 'Mettre à Jour'}) }}<br>

   <br> <a href="{{ path('app_event_calendar_index') }}"> <button class="btn btn-info">Reour en Arrière</button></a><br>

    {{ include('event_calendar/_delete_form.html.twig') }}
{% endblock %}
