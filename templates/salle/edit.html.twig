{% extends getRole(app.request) == "ROLE_ARTISTE" ? "base-back-artiste.html.twig" :  getRole(app.request) == "ROLE_PRESTATAIRE"  ? 'base-back-prest.html.twig' : 'base-back.html.twig' %}


{% block title %}Edit Salle{% endblock %}

{% block body %}

    <h1 class="text-primary" style="margin-left: 35%">Modifier Salle</h1>

    {{ include('salle/_form.html.twig', {'button_label': 'Mettre à Jour'}) }}

   <br> <a href="{{ path('app_salle_index') }}"><button class="btn btn-info">Reour en Arrière</button></a><br>

   <br> {{ include('salle/_delete_form.html.twig') }}
{% endblock %}
