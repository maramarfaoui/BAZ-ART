{% extends getRole(app.request) == "ROLE_ARTISTE" ? "base-back-artiste.html.twig" :  getRole(app.request) == "ROLE_PRESTATAIRE"  ? 'base-back-prest.html.twig' : 'base-back.html.twig' %}



{% block title %}Salle{% endblock %}





{% block body %}
<h1 class="text-primary" style="margin-left: 35%">Salles</h1>

<table class="table table-bordered">
    <tbody>
    <tr>
        <th class="h5">Id</th>
        <td class="h4">{{ salle.id }}</td>
    </tr>
    <tr>
        <th class="h5">Nom</th>
        <td class="h4">{{ salle.nom }}</td>
    </tr>
    <tr>
        <th class="h5">Type</th>
        <td class="h4">{{ salle.type }}</td>
    </tr>
    <tr>
        <th class="h5">Statu</th>
        <td class="h4">{{ salle.statu }}</td>
    </tr>
    <tr>
        <th class="h5">Prix</th>
        <td class="h4">{{ salle.prix }}</td>
    </tr>
    <tr>
        <th>Capacite</th>
        <td class="h4">{{ salle.capacite }}</td>
    </tr>

    <tr>
        <th class="h5">Image</th>
        <td class="h4"><img class="img-fluid"></td>
    </tr>
    </tbody>
</table>
    <a href="{{ path('app_salle_index') }}"><button class="btn btn-info">En Arrière</button></a><br>

   <br> <a href="{{ path('app_salle_edit', {'id': salle.id}) }}"><button class="btn btn-info">Modifier</button></a><br>

   <br> {{ include('salle/_delete_form.html.twig') }}
{% endblock %}
