<form method="post" action="{{ path('app_resnew_delete', {'idRes': re.idRes}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ re.idRes) }}">
    <button class="btn btn-info">Supprimer</button>
</form>
